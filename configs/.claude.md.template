# Claude Code 项目规范

## 上下文

<TODO: 项目简介、技术栈、目标用户>

## 工作方式

1. 先阅读 `AGENTS.md` 了解完整规范。
2. 复杂任务使用 `create-plan` skill 制定计划。
3. 修改代码前，使用 `codebase-recon` skill 了解项目历史风险。
4. 完成后使用 `code-review` skill 自检。

## 禁止事项

- 不要修改未明确要求修改的文件。
- 不要删除没有备份的代码。
- 不要假设不存在的外部依赖。
